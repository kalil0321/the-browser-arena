import { SidebarInset } from "@/components/ui/sidebar";

export default function NotFound() {
    return (
        <SidebarInset className="relative flex flex-1 flex-col items-center justify-center overflow-hidden">
            404 - Lost in the arena
        </SidebarInset>
    );
}